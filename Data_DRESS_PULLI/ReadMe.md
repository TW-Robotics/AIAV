### Classification Data extractor
Takes two Strings as input and extracts the two class images and seperates them into new folder. Can be used for binary classification usecases with a non-binary data set
